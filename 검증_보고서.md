# 검증 보고서 — DeepL GitHub/Render 배포 전환

## 대상

- 작업 폴더: `C:\Users\user\Desktop\GPT 작업\20260806_093816_NZ교류신문_OpenAI_배포앱`
- 번역 provider: DeepL API
- 기본 endpoint: `https://api-free.deepl.com`
- 실제 API 키: 실행 환경에 없음

## 코드 검증

| 항목 | 결과 |
|---|---|
| `python3 -m unittest discover -s tests -v` | 4개 통과 |
| `python3 -m compileall -q app.py tests` | 통과 |
| `node --check board-autofill-extension/content.js` | 통과 |
| `static/manifest.json` 파싱 | 통과 |
| `board-autofill-extension/manifest.json` 파싱 | 통과 |
| `render.yaml` YAML 파싱 | 통과 |
| `.github/workflows/ci.yml` YAML 파싱 | 통과 |
| 실제 키 패턴 저장 여부 검사 | 미검출 |

## 로컬 HTTP 검증

목업 모드로 Gunicorn을 실행했다.

```text
MOCK_TRANSLATION=1 PORT=5087 .venv/bin/gunicorn app:app --bind 127.0.0.1:5087
```

확인 결과:

- `GET /api/health` → HTTP 200
- `GET /` → HTTP 200
- `POST /api/translate` 한국어 입력 → HTTP 200, `provider: DeepL`, target `en`, `ru`
- health 응답에 `deepl_configured`, `mock_mode`, `api_base`만 표시되고 키 값은 없음
- 명시되지 않은 Origin은 CORS 허용 헤더를 받지 않음
- 허용 목록에 등록한 Origin만 `Access-Control-Allow-Origin`을 받음

## 브라우저 검증

- 페이지 제목: `안성초등학교 국제교류 신문`
- 상단 상태: `DeepL 준비됨 · Free`
- 제목·본문 입력 후 `DeepL로 자동번역` 클릭
- 두 번역 영역에 목업 결과 표시
- 번역 완료 후 버튼이 다시 활성화됨
- 브라우저 콘솔 오류 0건

## 실제 DeepL 호출

현재 실행 환경에 실제 `DEEPL_API_KEY`가 없기 때문에 DeepL 서버에 대한 실호출은 실행하지 않았다. 배포 전에 Render Environment Variables에 아래 값을 직접 입력해야 한다.

```text
DEEPL_API_KEY=<실제 키>
DEEPL_API_BASE_URL=https://api-free.deepl.com
```

Pro 계정은 `https://api.deepl.com`을 사용한다. 실제 키를 채팅·README·GitHub 파일에 기록하지 않는다.

## 배포 검증 경계

로컬 폴더는 Git 저장소로 초기화하고 `main` 브랜치에 파일을 스테이징했지만, 현재 환경에는 GitHub 원격·`gh` CLI 인증이 없어 Push와 Render 공개 URL 확인은 아직 하지 못했다. 따라서 공개 URL이 생성됐다고 주장하지 않는다.

다음 1회 작업은 사용자 GitHub 계정으로 빈 저장소를 만든 뒤 README의 `git remote add origin ...` 및 `git push -u origin main`을 실행하고, Render Blueprint로 연결하는 것이다.
