# 작업 요약

## 요청

`20260806_093816_NZ교류신문_OpenAI_배포앱`의 DeepL API 연동을 안전하게 유지하면서 GitHub 저장소를 통해 Render에 배포할 수 있는 형태로 정리한다.

## 변경된 산출물

- `app.py`
  - DeepL `/v2/translate` 서버 호출 유지
  - `DEEPL_API_KEY`는 서버 환경변수에서만 읽음
  - Free/Pro endpoint 선택 및 `/api/health` 상태 표시
  - 명시적으로 허용한 `ALLOWED_ORIGINS`만 CORS 허용
  - 입력 길이 제한과 기본 IP별 rate limit 유지
- `static/index.html`
  - 브라우저는 `/api/translate`만 호출
  - OpenAI 시절 payload 저장 키를 `nz-exchange-news-payload-v1`로 정리
- `board-autofill-extension/content.js`
  - 편집기와 동일한 payload 저장 키 사용
- `render.yaml`
  - Render Web Service용 build/start 명령
  - `/api/health` health check
  - `autoDeploy: true`
  - DeepL 키·endpoint·CORS·제한값 환경변수 선언
- `.github/workflows/ci.yml`
  - main Push와 PR에서 목업 번역 테스트, Python compile, JS/manifest 검사, 키 패턴 검사
- `DEPLOYMENT.md`
  - GitHub → Render 배포 순서와 비밀키 보관 원칙
- `README.md`
  - GitHub 저장소 초기화/Push 및 Render Blueprint 배포 절차 갱신
- `.gitignore`
  - `.env`, `.venv`, 캐시 및 컴파일 산출물 제외

## 검증

- Python unittest: 4개 통과
- Python compileall: 통과
- `node --check board-autofill-extension/content.js`: 통과
- 두 manifest JSON 파싱: 통과
- `render.yaml`, `.github/workflows/ci.yml` YAML 파싱: 통과
- secret-pattern 검사: 통과
- Gunicorn으로 `127.0.0.1:5087` 실행 후 `/api/health` HTTP 200 확인
- 실행 중 브라우저에서 화면 로드, DeepL Free 상태 표시, 제목·본문 목업 번역 완료 확인
- 브라우저 콘솔 JavaScript 오류: 0건
- 실제 DeepL 호출: 실행 환경에 실제 키가 없어 미실행

## 배포 방식

GitHub Pages 단독으로는 DeepL 서버 키를 안전하게 보관할 수 없으므로, GitHub 저장소를 Render Web Service에 연결하는 단일 서비스 방식을 기본값으로 한다.

```text
GitHub repository
  -> Render Blueprint (render.yaml)
  -> Flask + Gunicorn
  -> DeepL API (DEEPL_API_KEY: Render secret)
```

## 미완료/사용자 측 1회 작업

현재 WSL 환경에는 `gh` CLI, GitHub 인증, 원격 저장소가 없어 GitHub 원격 생성·Push·Render 연결은 실행하지 못했다. 사용자는 README/DEPLOYMENT.md의 절차대로 빈 GitHub 저장소를 만든 뒤 Push하고, Render에서 `DEEPL_API_KEY`를 Environment Variable로 직접 등록하면 된다. 실제 DeepL 키는 채팅이나 GitHub에 입력하지 않는다.

## Obsidian 기록

- `04_코딩프로젝트/NZ 교류신문 DeepL GitHub Render 배포 전환 기록.md`
- MOC: `04_코딩프로젝트/📌 코딩프로젝트 MOC.md`
